Wishes (not prioritized)

- tls support mutual auth
- web hook async response (so responding on url in request object)
- SSE support
- control API (for example increasing response times after some time, for performance testing). or with a DSL for more static control.

